const STATE_READY = 'ready',
    STATE_ERROR = 'error',
    STATE_LOADING = 'loading',
    STATE_SUCCESS = 'success',
    STATE_TIMEOUT = 'timeout';

export {
    STATE_READY,
    STATE_ERROR,
    STATE_LOADING,
    STATE_SUCCESS,
    STATE_TIMEOUT
};